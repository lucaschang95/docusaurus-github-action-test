# branch

> List, create, or delete branches

## syntax

### 查看本地分支

`git branch` 或者 `git branch --list`

### 查看远程分支

`git branch -r`

### 本地分支 + 远程分支

`git branch -a`

### 删除分支

- `git branch -d <branch>`
- `git branch -D <branch>`