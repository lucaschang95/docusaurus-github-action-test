# list

> 等同与 `yarn why`

```shell
# local packages
npm list
# global packages
npm list -g
npm list --depth=0
# You can get the version of a specific package by specifying its name:
npm list cowsay
# also work for dependencies of packages you installed
npm list minimist
```
