# input redirection

to redirect a file into a command, 变成 stream

## 语法

sort < words.txt

## output redirection

输出重定向

```bash
cat files.txt
```

## pipe

```bash
cat words.txt | grep [cC] | sort -r
```
