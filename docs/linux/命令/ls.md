# ls

## 语法

```bash
ls [flags] [directory]

ls # List files in the current working directory

ls ~ # ls the home dir
ls ..
ls /

ls -1 # one line per file

ls -l # show long listing information
ls -lh # show long listing information human readable
ls -a # display all file (include hidden ones)
```
